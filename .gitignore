# NEVER commit this. Real credentials and account identifiers live here.
cloud.env

# DVC manages these; Git must not.
data/raw/
data/_local_blob/
.dvc/cache/

# Local tracking store
mlruns/
mlartifacts/
mlflow.db

reports/*.json
reports/*.joblib
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Excel lock files — created while the specification workbook is open.
~$*

# ruff cache
.ruff_cache/

# Instructor material lives in the private teaching repository. If this path ever
# reappears here it is a mistake, not a convenience.
instructor/
